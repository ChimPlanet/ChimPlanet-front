import styled from "styled-components";

export default function ContentsTags() {
    return(
        <Container>
            태그
        </Container>
    );
};

const Container = styled.div`
    height: 100vh;
`;